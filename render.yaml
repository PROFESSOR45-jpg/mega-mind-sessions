services:
  - type: web
    name: mega-mind-sessions
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: ADMIN_KEY
        generateValue: true
